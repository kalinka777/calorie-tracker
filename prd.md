# PRD: «Калории по фото» — минималистичный трекер питания

**Версия:** 1.0
**Тип продукта:** одностраничное веб-приложение (PWA-ready)
**Платформы:** мобильный браузер (приоритет), десктоп

---

## 1. Цель продукта

Сделать учёт калорий настолько простым, чтобы он занимал **не больше 10 секунд на приём пищи**. Пользователь фотографирует блюдо, AI оценивает калорийность и БЖУ, пользователь нажимает «Сохранить». Никаких баз продуктов, поиска и ручного ввода граммовок.

### Ключевые принципы
- **Фото — главный способ ввода**, текст — запасной.
- **Минимум кликов:** путь «фото → сохранить» занимает 2 нажатия.
- **Спокойный интерфейс:** ничего не отвлекает и не тревожит.
- **Приватность:** данные хранятся только на устройстве пользователя.

### Метрики успеха
| Метрика | Цель |
|---|---|
| Время добавления записи по фото | ≤ 10 с |
| Количество нажатий на основной сценарий | 2 |
| Доля успешно распознанных фото | ≥ 90 % |
| Работоспособность без API-ключа (mock) | 100 % функций UI |

---

## 2. Целевая аудитория

- Люди, которые хотят следить за питанием, но бросали классические трекеры из-за рутины.
- Пользователи, которым важна примерная оценка, а не точность до грамма.
- Технически продвинутые пользователи, готовые один раз вставить свой API-ключ.

---

## 3. Пользовательские сценарии

### 3.1. Добавление по фото (основной)
1. Пользователь открывает приложение и нажимает крупную кнопку «Сфотографировать блюдо».
2. На телефоне открывается камера, на ПК — выбор файла (также работает drag & drop).
3. Фото сжимается на клиенте и отправляется в AI.
4. Появляется карточка подтверждения со скелетоном загрузки.
5. Карточка показывает название, вес, ккал и БЖУ.
6. При необходимости пользователь правит вес, и значения пересчитываются.
7. Нажатие «Сохранить» добавляет запись в историю и обновляет прогресс.

### 3.2. Текстовый ввод
1. Пользователь пишет в поле «2 яйца и кофе с молоком» и нажимает Enter.
2. Текст отправляется в AI, дальше как в шагах 4–7 выше (без миниатюры, вместо неё иконка).

### 3.3. Удаление записи
- На мобильном: свайп влево по записи.
- На ПК: иконка корзины при наведении.
- Появляется toast «Запись удалена · Отменить» (5 секунд).

### 3.4. Первый запуск
- Приложение сразу работает в **mock-режиме**.
- Ненавязчивая плашка: «Демо-режим. Добавьте API-ключ в настройках для реального распознавания».

---

## 4. Функциональные требования

### 4.1. Главный экран (сверху вниз)

| Блок | Описание |
|---|---|
| Шапка | Дата («Сегодня, 24 сентября»), иконка настроек справа |
| Прогресс дня | «Съедено 1 240 · Осталось 760 ккал», тонкая полоса, мини-сводка Б/Ж/У |
| Кнопка фото | Крупная, по центру, с иконкой камеры |
| Поле ввода | Одна строка, плейсхолдер «Или опишите, что съели…» |
| История | Список записей за сегодня, новые сверху |

**Состояние превышения нормы:** полоса прогресса окрашивается в приглушённый терракотовый цвет (не красный), текст меняется на «Сверх нормы на X ккал».

**Пустое состояние истории:** короткий текст «Пока ничего. Сфотографируйте первый приём пищи».

### 4.2. Карточка подтверждения (bottom sheet)
- Миниатюра фото (если есть).
- Название блюда (редактируемое).
- Вес в граммах (редактируемый; ккал и БЖУ пересчитываются пропорционально).
- Ккал крупно, Б/Ж/У мелко.
- Индикатор уверенности AI при низкой уверенности: «Оценка приблизительная».
- Кнопки: **«Сохранить»** (основная) и «Отмена».
- Состояния: загрузка (скелетон), результат, ошибка (текст ошибки и кнопка «Повторить»).

### 4.3. AI-интеграция

**Провайдеры:**
| Провайдер | Модель по умолчанию | Вызов |
|---|---|---|
| OpenAI | `gpt-4o-mini` (опция `gpt-4o`) | `POST https://api.openai.com/v1/chat/completions` |
| Google Gemini | `gemini-flash` (актуальная версия) | `POST generativelanguage.googleapis.com/.../generateContent` |
| Mock | — | Локальная функция, задержка ~1.5 с |

**Подготовка изображения:**
- Для запроса: ресайз до 1024 px по большей стороне, JPEG quality 0.8, base64.
- Для хранения: миниатюра ~200 px, JPEG quality 0.7.

**Системный промт для модели (основа):**
> Ты — нутрициолог. Определи еду на изображении (или в тексте), оцени вес порции и пищевую ценность. Отвечай ТОЛЬКО валидным JSON без пояснений и без markdown по схеме ниже. Названия на русском.

**Схема ответа:**
```json
{
  "name": "Омлет с тостом",
  "items": [
    { "name": "Омлет из 2 яиц", "grams": 120, "kcal": 180, "protein": 13, "fat": 14, "carbs": 1 },
    { "name": "Тост пшеничный", "grams": 30, "kcal": 80, "protein": 3, "fat": 1, "carbs": 15 }
  ],
  "total": { "grams": 150, "kcal": 260, "protein": 16, "fat": 15, "carbs": 16 },
  "confidence": "medium"
}
```

**Обработка ответа:**
- Удалить обёртку ```` ```json ```` при наличии, выполнить `JSON.parse` в try/catch.
- Проверить обязательные поля `name` и `total.kcal`; числа округлить.
- Если на фото нет еды, модель возвращает `{"error": "no_food"}`, и UI показывает «Не удалось найти еду на фото».
- Сетевые ошибки и ошибки 401/429 выводятся понятным текстом («Неверный ключ», «Превышен лимит запросов»).

### 4.4. Настройки (модальное окно)
- Дневная норма ккал (по умолчанию 2000).
- Провайдер AI: Mock / OpenAI / Gemini.
- API-ключ: поле `type=password`, кнопка «показать». Предупреждение: «Ключ хранится только в этом браузере и отправляется напрямую провайдеру».
- Экспорт данных в JSON и импорт из JSON.
- «Очистить все данные» с подтверждением.

### 4.5. Хранение данных (localStorage)

| Ключ | Содержимое |
|---|---|
| `settings` | `{ dailyGoal, provider, apiKey, model }` |
| `entries:YYYY-MM-DD` | Массив записей за день |

**Модель записи:**
```json
{
  "id": "uuid",
  "createdAt": "2026-09-24T08:15:00",
  "source": "photo | text",
  "name": "Омлет с тостом",
  "grams": 150,
  "kcal": 260,
  "protein": 16,
  "fat": 15,
  "carbs": 16,
  "thumb": "data:image/jpeg;base64,... | null",
  "note": "исходный текст для source=text"
}
```

**Правила:**
- Дата определяется по локальному времени устройства; после полуночи начинается новый день.
- При `QuotaExceededError` приложение предлагает удалить миниатюры старше 30 дней (сами записи сохраняются).
- Все операции чтения и записи обёрнуты в try/catch.

---

## 5. Дизайн-система

### 5.1. Палитра
| Токен | Светлая тема | Тёмная тема | Назначение |
|---|---|---|---|
| `bg` | `#F7F6F3` | `#1C1C1E` | Фон |
| `surface` | `#FFFFFF` | `#2A2A2C` | Карточки |
| `text` | `#2F3437` | `#ECECEC` | Основной текст |
| `muted` | `#8B8E91` | `#9A9A9E` | Вторичный текст |
| `border` | `#E8E6E1` | `#3A3A3C` | Разделители |
| `accent` | `#8A9A5B` | `#A3B18A` | Кнопки, прогресс |
| `accent-soft` | `#E9EDDF` | `#34392B` | Фон акцентных зон |
| `warn` | `#C9896B` | `#D39B80` | Превышение нормы |

Тёмная тема включается автоматически через `prefers-color-scheme`.

### 5.2. Типографика и формы
- Системный шрифт: `-apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif`.
- Размеры: крупная цифра прогресса 28–32 px, основной текст 15–16 px, подписи 13 px.
- Скругления: карточки 16–20 px, кнопки 14 px, главная кнопка до 24 px.
- Тени мягкие и едва заметные (`0 1px 3px rgba(0,0,0,.06)`).
- Анимации 150–250 мс, ease-out; учитывать `prefers-reduced-motion`.

### 5.3. Адаптивность
- Мобильные от 360 px: контент на всю ширину с отступами 16 px.
- Десктоп: колонка по центру, `max-width: 480px`.
- Области нажатия не меньше 44×44 px.

---

## 6. Нефункциональные требования

| Требование | Критерий |
|---|---|
| Архитектура | Один `index.html`, Tailwind CDN, чистый JS без сборки |
| Производительность | Первая отрисовка < 1 с, отклик UI < 100 мс |
| Доступность | WCAG AA по контрасту, `aria-label` на иконках, управление с клавиатуры |
| Приватность | Никаких внешних запросов, кроме выбранного AI-провайдера и Tailwind CDN |
| Надёжность | Приложение не падает при битом localStorage или некорректном ответе AI |
| Код | Разделы state / storage / ai / ui, комментарии на русском |

---

## 7. Ограничения и риски

| Риск | Решение |
|---|---|
| API-ключ хранится в браузере и виден в запросах | Предупреждение в настройках; для продакшена нужен серверный прокси (вне рамок v1) |
| Лимит localStorage ~5 МБ | Сжатие миниатюр, очистка старых фото, экспорт |
| Неточность AI-оценки | Редактирование веса, индикатор уверенности |
| Ответ AI не в JSON | Устойчивый парсинг, повтор запроса |
| CORS у провайдера | Использовать официальные REST-эндпоинты, которые поддерживают вызов из браузера; при ошибке показывать подсказку |

---

## 8. Вне рамок v1 (roadmap)

- Статистика за неделю и месяц, графики.
- Расчёт нормы по параметрам (рост, вес, цель).
- Цели по БЖУ.
- Избранные и повторяющиеся блюда («как вчера»).
- Голосовой ввод.
- PWA: установка на экран, офлайн-кэш.
- Облачная синхронизация и серверный прокси для ключа.

---

## 9. Критерии приёмки v1

- [ ] Без API-ключа приложение работает в mock-режиме, все сценарии проходятся.
- [ ] С ключом OpenAI или Gemini фото распознаётся, результат отображается в карточке.
- [ ] Текстовый ввод работает через тот же AI-пайплайн.
- [ ] Изменение веса в карточке пересчитывает ккал и БЖУ.
- [ ] Прогресс дня обновляется сразу после сохранения и удаления.
- [ ] Удаление работает свайпом (мобильные) и кнопкой (ПК), отмена доступна 5 секунд.
- [ ] После перезагрузки страницы данные сохраняются.
- [ ] После полуночи начинается новый день.
- [ ] Интерфейс корректен на ширине 360 px и 1440 px, в светлой и тёмной темах.
- [ ] Ошибки сети, ключа и парсинга выводятся понятными сообщениями.
